# ADAA Public Replication Package v1.0.1 — Publication Sync

**Release class:** patch release; no scientific recomputation or methodological change.

Changes from v1.0:
- synchronize the active exhibit set to the 12 figures embedded in working paper v1.15;
- add final Figure 6 cumulative-wealth source data and render;
- remap stress/failure evidence to final Figure 8;
- add final-paper publication source copies for Figures 5–8 and Tables 2A/3 where wording changed;
- retain frozen internal labels in code while documenting the final public-label mapping;
- add the exact paper-v1.15 target hashes;
- add GitHub clone/run guidance for frozen scripts that require the execution root to be named `05_ADAA`;
- replace the stale v1.0 static file-count record with a non-circular v1.0.1 validation record and coverage-complete manifest.

All research results, strategy rules, target weights, sample windows, statistics and conclusions are unchanged.
